<h1>Cities</h1>
<%= link_to "New city", new_city_path %>
<hr>
<%= paginate @cities %>

<div id="cities">
  <% @cities.each do |city| %>
    <%= render city %>
    <p>
      <%= link_to "Show this city", city %>
    </p>
<hr class="hr-thick">
  <% end %>
  <hr>
</div>
